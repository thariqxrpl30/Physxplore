<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">


    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Lomba"
        tools:targetApi="31">

        <!-- Splash Activity sebagai tampilan awal -->
        <activity android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".LoginActivity" />
        <activity android:name=".SelectLevelActivity" />
        <activity android:name=".LabolatoryActivity" />
        <activity android:name=".FizzyActivity" />
        <activity android:name=".VideoActivity" />
        <activity android:name=".MateriActivity" />
        <activity android:name=".SoalActivity" />
        <activity android:name=".Soal1Activity" />

    </application>



</manifest>